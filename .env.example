# Gemini AI configuration
# GEMINI_API_KEY=MY_GEMINI_API_KEY

# Production DIV EDIT AI FFmpeg renderer.
# Must be an HTTPS base URL, for example https://ffmpeg.example.com
# Leave empty until the backend is deployed; the app will fail exports explicitly.
DIV_EDIT_FFMPEG_BASE_URL=
