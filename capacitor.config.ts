import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ng.divstudio.magiceditor',
  appName: 'Magic Editor',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
